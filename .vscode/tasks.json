{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "compile",
            "type": "process",
            "command": "clang ${file} -o main",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

 